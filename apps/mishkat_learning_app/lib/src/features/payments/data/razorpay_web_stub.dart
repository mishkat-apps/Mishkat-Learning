class RazorpayJS {
  RazorpayJS(dynamic options);
  void open() {
    throw UnimplementedError('RazorpayJS is only available on web');
  }
  void on(String event, Function callback) {
    throw UnimplementedError('RazorpayJS is only available on web');
  }
}
